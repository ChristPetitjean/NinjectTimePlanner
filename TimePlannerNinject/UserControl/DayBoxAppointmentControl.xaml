<UserControl x:Class="TimePlannerNinject.UserControl.DayBoxAppointmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Tag="{Binding Path=ID}">
   <Border x:Name="BorderElement" CornerRadius="5" BorderBrush="DarkOliveGreen" Background="LightGreen" BorderThickness="1" Margin="2,2,2,1">
      <StackPanel>
         <TextBlock x:Name="DisplayText" Text="{Binding Path=IdWorkPlace, FallbackValue='Lieu de travail'}" FontSize="10" FontWeight="Bold" Padding="2" TextWrapping="Wrap"/>
         <TextBlock x:Name="StartDate" Text="{Binding Path=WorkStartTime, FallbackValue='08:00'}" FontSize="10" Padding="2" TextWrapping="Wrap"/>
         <TextBlock x:Name="EndDate" Text="{Binding Path=WorkEndTime, FallbackValue='18:00'}" FontSize="10" Padding="2" TextWrapping="Wrap"/>
      </StackPanel>
   </Border>
</UserControl>
